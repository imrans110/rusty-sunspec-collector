[package]
name = "types"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared types for the SunSpec collector"

[dependencies]
serde = { workspace = true, features = ["derive"] }
